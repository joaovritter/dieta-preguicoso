# O frontend fala sempre com o caminho relativo /api.
# Em dev o proxy do Vite (vite.config.ts) manda para http://localhost:3001.
# Em produção o Nginx faz o proxy. Não há variável de ambiente obrigatória.
#
# Porta do servidor de desenvolvimento (opcional):
# PORT=5173
