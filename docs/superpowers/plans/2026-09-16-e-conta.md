# Conta: trocar senha e excluir conta — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Modelo:** cada tarefa é implementada por subagente **Sonnet** (`model: "sonnet"`); a sessão Opus só coordena e revisa. Não executar antes de o usuário liberar.

**Goal:** Dar à pessoa um jeito de trocar a senha e de apagar a conta inteira (banco + fotos/áudios no disco), a partir da seção CONTA do perfil.

**Architecture:** Dois endpoints novos em `backend/src/routes/me.ts` (`PUT /api/me/senha`, `DELETE /api/me`), com SQL em `repos/usuarios.ts`, novo código de erro `SENHA_INCORRETA` (400) e limite de tentativas por usuário. A exclusão roda numa transação que lê as `midia_url` e apaga o usuário (os `ON DELETE CASCADE` levam o resto); depois do commit os arquivos saem do disco. No frontend: subtela `/perfil/senha` e um diálogo MUI de "excluir conta" que pede a senha e faz logout.

**Tech Stack:** Express 5 + Zod 4 + `pg` + bcryptjs (backend, Vitest); React 19 + MUI 9 + react-router 7 (frontend, Vitest em ambiente `node`).

**Spec:** `docs/superpowers/specs/2026-09-16-redesign-liquid-glass-design.md` (seções "Conta", "Telas › Perfil", "Casca do app e rotas").

**Interfaces que vêm do plano B (`docs/superpowers/plans/2026-09-16-b-telas.md`, Tasks 2, 9 e 10 — já executado):**
- `frontend/src/App.tsx` registra as subtelas do perfil como rotas protegidas **irmãs** da `Casca` (sem tab bar), no formato `<Route path="/perfil/metas" element={<Protegida><MetasPage /></Protegida>} />`. As páginas moram em `frontend/src/pages/perfil/` com sufixo `Page` (`MetasPage`, `DadosPage`, `RefeicoesPage`). A rota nova segue o mesmo formato.
- `SubTela({ titulo: string; voltarPara: string; children: ReactNode; rodape?: ReactNode })` em `frontend/src/components/perfil/SubTela.tsx` — `← voltar`, título 800 32px, conteúdo com gap 18, `rodape` no fim.
- `SecaoConta({ antesDeSair?: ReactNode; depoisDeSair?: ReactNode })` em `frontend/src/components/perfil/SecaoConta.tsx` — renderiza `CONTA` e a linha `sair`; `sair` só é `ultima` quando `depoisDeSair` não vem. `pages/Perfil.tsx` hoje renderiza `<SecaoConta />` como último filho de `<Tela gap={26}>`.
- Primitivas em `frontend/src/components/ui/`: `LinhaLista({ rotulo: ReactNode; valor?: ReactNode; chevron?: boolean; cor?: 'normal' | 'perigo'; onClick?: () => void; ultima?: boolean; children?: ReactNode })`, `BotaoCta(ButtonBaseProps & { variante?: 'primario' | 'contorno' | 'perigo'; altura?: number })`. `Erro({ mensagem, aoFechar? })` em `components/Erro.tsx`.
- `useConfirmacao()` só tem título/texto/rótulo — não aceita campo de senha. O diálogo de excluir conta é um `Dialog` próprio com o **mesmo** `Paper` (`borderRadius 20px`, `p 22px`, `m 22px`, `bgcolor background.default`, `backgroundImage none`) e os mesmos botões `BotaoCta`.
- Campos de formulário das subtelas do perfil são `TextField` do MUI (tema do plano A); este plano usa o mesmo.
- A tela `Perfil` não tem mecanismo de aviso vindo de navegação (o `aviso` dela é estado local do "copiar"). Por isso trocar a senha só volta para `/perfil`, sem mensagem.
- Frontend já tem `npm test` (`vitest run`, ambiente `node`) — criado no plano A.
- Nenhum dos planos A–D mexe em `routes/me.ts`, `repos/usuarios.ts`, `lib/erros.ts` ou `lib/uploads.ts`.

## Global Constraints

- Contrato primeiro: `docs/api-contract.md` é atualizado antes do código; divergência é bug.
- `PUT /api/me/senha` body `{ senha_atual: string, senha_nova: string (8–200) }` → `204`; `400 SENHA_INCORRETA` ("senha atual incorreta") — **não** 401, que derruba a sessão no frontend.
- `DELETE /api/me` body `{ senha: string }` → `204`; `400 SENHA_INCORRETA`. Apaga o usuário e, depois do commit, remove do disco os arquivos de `midia_url` dele (falha ao remover arquivo é logada, não quebra a resposta). Frontend faz logout em seguida.
- Toda entrada validada com Zod antes de tocar no banco; erros via `AppError`; SQL puro em `repos/`.
- Testes Vitest só de lógica pura, sem rede e sem banco.
- Frontend sem CSS puro: MUI (`sx`/`styled`) e tokens do tema (`theme.vars.palette.*`).
- Textos da interface em minúsculas, português, no tom do app.
- Campos: `TextField` do MUI, como nas subtelas do plano B. CTA: `BotaoCta` do plano B (`min-height 52px`, raio 13 px, 700 15px). Linhas de lista: `LinhaLista` do plano B.
- Commits em português, terminando com as linhas de atribuição do repositório.

---

### Task 1: Contrato + código de erro `SENHA_INCORRETA`

**Files:**
- Modify: `docs/api-contract.md:14-16` (lista de códigos) e depois do bloco `### PUT /api/me` (linha ~104)
- Modify: `backend/src/lib/erros.ts:1-32`

**Interfaces:**
- Produces: `CodigoErro` inclui `'SENHA_INCORRETA'` com status padrão `400`.

- [ ] **Step 1: Atualizar a lista de códigos no contrato**

Em `docs/api-contract.md`, trocar:

```md
  `NAO_ENCONTRADO`, `IA_INDISPONIVEL`, `IA_RESPOSTA_INVALIDA`, `ARQUIVO_INVALIDO`, `ERRO_INTERNO`,
  `SEM_ACESSO`, `PEDIDO_DUPLICADO`, `REFEICAO_EM_USO`.
```

por:

```md
  `NAO_ENCONTRADO`, `IA_INDISPONIVEL`, `IA_RESPOSTA_INVALIDA`, `ARQUIVO_INVALIDO`, `ERRO_INTERNO`,
  `SEM_ACESSO`, `PEDIDO_DUPLICADO`, `REFEICAO_EM_USO`, `SENHA_INCORRETA`, `LIMITE_EXCEDIDO`.
```

- [ ] **Step 2: Documentar os dois endpoints**

Logo depois do bloco `### PUT /api/me` (termina em "→ `200 Perfil` (já com metas recalculadas se `metas_automaticas`).") e antes de `### GET /api/refeicoes`, inserir:

```md
### `PUT /api/me/senha`
Body: `{ senha_atual: string, senha_nova: string }` (`senha_nova` com 8–200 chars) → `204`
`400 SENHA_INCORRETA` (senha atual não confere) · `400 VALIDACAO` ·
`429 LIMITE_EXCEDIDO` (10 tentativas por 15 min por usuário, somando este endpoint e `DELETE /api/me`).
Não é 401 de propósito: 401 derruba a sessão no cliente. O token atual continua valendo.

### `DELETE /api/me`
Body: `{ senha: string }` → `204` · `400 SENHA_INCORRETA` · `400 VALIDACAO` · `429 LIMITE_EXCEDIDO`
Apaga a conta de vez: registros, água, refeições, amizades, pedidos, participação em grupos,
**grupos que a pessoa criou (mesmo com outros membros)**, curtidas e comentários. As fotos e áudios
dos registros são removidos do disco depois que o banco confirma; se algum arquivo falhar, fica no
log e a resposta continua `204`. Não tem volta — o cliente faz logout em seguida.
```

- [ ] **Step 3: Adicionar o código no backend**

Em `backend/src/lib/erros.ts`, adicionar `| 'SENHA_INCORRETA'` depois de `| 'CREDENCIAIS_INVALIDAS'` no tipo, e no mapa:

```ts
  CREDENCIAIS_INVALIDAS: 401,
  // 400, não 401: quem erra a senha atual continua logado.
  SENHA_INCORRETA: 400,
```

- [ ] **Step 4: Typecheck**

Run (em `backend/`): `npm run typecheck`
Expected: sai sem erros (o `Record<CodigoErro, number>` obriga a entrada no mapa — se faltar, falha aqui).

- [ ] **Step 5: Commit**

```bash
git add docs/api-contract.md backend/src/lib/erros.ts
git commit -m "Contrato de trocar senha e excluir conta, com erro SENHA_INCORRETA"
```

---

### Task 2: Nome de arquivo de mídia como função pura testada

`caminhoDaMidia` em `lib/uploads.ts` já barra travessia de diretório, mas o módulo importa `env` e cria a pasta ao carregar — não dá para testar sem ambiente. A exclusão de conta vai apagar muitos arquivos de uma vez com base em valores do banco, então a regra de segurança ganha teste.

**Files:**
- Create: `backend/src/lib/midia.ts`
- Create: `backend/src/lib/midia.test.ts`
- Modify: `backend/src/lib/uploads.ts:78-86`

**Interfaces:**
- Produces: `nomeDaMidia(midiaUrl: string): string | null` em `backend/src/lib/midia.ts`. `caminhoDaMidia(midiaUrl)` em `uploads.ts` mantém a assinatura.

- [ ] **Step 1: Escrever o teste que falha**

`backend/src/lib/midia.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { nomeDaMidia } from './midia.js';

describe('nomeDaMidia', () => {
  it('extrai o nome de uma url servida por /uploads', () => {
    expect(nomeDaMidia('/uploads/3f2b9c1e-aaaa-bbbb-cccc-1234567890ab.jpg')).toBe(
      '3f2b9c1e-aaaa-bbbb-cccc-1234567890ab.jpg',
    );
  });

  it('recusa url que não é nossa', () => {
    expect(nomeDaMidia('https://exemplo.com/foto.jpg')).toBeNull();
    expect(nomeDaMidia('/outra/foto.jpg')).toBeNull();
  });

  it('recusa nome vazio', () => {
    expect(nomeDaMidia('/uploads/')).toBeNull();
  });

  it('recusa travessia de diretório', () => {
    expect(nomeDaMidia('/uploads/../.env')).toBeNull();
    expect(nomeDaMidia('/uploads/..')).toBeNull();
    expect(nomeDaMidia('/uploads/sub/foto.jpg')).toBeNull();
    expect(nomeDaMidia('/uploads/..\\segredo')).toBeNull();
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run (em `backend/`): `npx vitest run src/lib/midia.test.ts`
Expected: FAIL — `Failed to resolve import "./midia.js"`.

- [ ] **Step 3: Implementar**

`backend/src/lib/midia.ts`:

```ts
const PREFIXO = '/uploads/';

/** Nome do arquivo de uma `midia_url` nossa, ou null se a URL não for nossa ou vier torta. */
export function nomeDaMidia(midiaUrl: string): string | null {
  if (!midiaUrl.startsWith(PREFIXO)) return null;
  const nome = midiaUrl.slice(PREFIXO.length);
  // Barra a travessia de diretório vinda de um valor gravado torto no banco.
  if (nome === '' || nome.includes('/') || nome.includes('\\') || nome.includes('..')) return null;
  return nome;
}
```

Em `backend/src/lib/uploads.ts`, adicionar o import `import { nomeDaMidia } from './midia.js';` junto dos outros e trocar a função inteira `caminhoDaMidia` por:

```ts
/** Caminho em disco de uma `midia_url`, ou null se a URL não for nossa. */
export function caminhoDaMidia(midiaUrl: string): string | null {
  const nome = nomeDaMidia(midiaUrl);
  return nome === null ? null : join(DIR_UPLOADS, nome);
}
```

- [ ] **Step 4: Rodar testes e typecheck**

Run (em `backend/`): `npx vitest run src/lib/midia.test.ts && npm run typecheck`
Expected: 4 testes PASS; typecheck sem erros.

- [ ] **Step 5: Commit**

```bash
git add backend/src/lib/midia.ts backend/src/lib/midia.test.ts backend/src/lib/uploads.ts
git commit -m "Extrai nomeDaMidia como função pura e cobre a trava de travessia com teste"
```

---

### Task 3: `PUT /api/me/senha`

**Files:**
- Modify: `backend/src/repos/usuarios.ts` (fim do arquivo)
- Modify: `backend/src/routes/me.ts`

**Interfaces:**
- Consumes: `SENHA_INCORRETA` (Task 1); `buscarPorId(id)`, `conferirSenha(senha, hash)`, `hashSenha(senha)`; `limiteTaxa`, `porUsuario`.
- Produces: `atualizarSenha(id: string, passwordHash: string): Promise<void>`; em `routes/me.ts`, `limiteConta` (middleware) e `exigirSenha(userId: string, senha: string): Promise<void>` — ambos reusados na Task 4.

- [ ] **Step 1: Repositório**

No fim de `backend/src/repos/usuarios.ts`:

```ts
export async function atualizarSenha(id: string, passwordHash: string): Promise<void> {
  await consultar('UPDATE users SET password_hash = $2 WHERE id = $1', [id, passwordHash]);
}
```

- [ ] **Step 2: Rota**

Em `backend/src/routes/me.ts`, trocar os imports do topo por:

```ts
import { Router } from 'express';
import { z } from 'zod';
import { perfilDe } from '../middleware/autenticar.js';
import { limiteTaxa, porUsuario } from '../middleware/limiteTaxa.js';
import { calcularMetas } from '../domain/nutricao.js';
import { timezoneValida } from '../domain/tempo.js';
import { OBJETIVOS, SEXOS } from '../domain/tipos.js';
import { conferirSenha, hashSenha } from '../lib/auth.js';
import { AppError } from '../lib/erros.js';
import {
  atualizarSenha,
  atualizarUsuario,
  buscarPorId,
  paraPerfil,
  type CamposAtualizaveis,
} from '../repos/usuarios.js';
```

E no fim do arquivo:

```ts
// Com um token vazado dá para chutar a senha atual por aqui: limita por usuário.
const limiteConta = limiteTaxa({
  janelaMs: 15 * 60 * 1000,
  maximo: 10,
  chave: porUsuario,
  mensagem: 'muitas tentativas com a senha, aguarde alguns minutos',
});

async function exigirSenha(userId: string, senha: string): Promise<void> {
  const linha = await buscarPorId(userId);
  const ok = linha ? await conferirSenha(senha, linha.password_hash) : false;
  if (!ok) throw new AppError('SENHA_INCORRETA', 'senha atual incorreta');
}

const trocaSenhaSchema = z.object({
  senha_atual: z.string().min(1, 'informe a senha atual').max(200),
  senha_nova: z
    .string()
    .min(8, 'a senha nova precisa de pelo menos 8 caracteres')
    .max(200, 'a senha nova pode ter no máximo 200 caracteres'),
});

rotasMe.put('/senha', limiteConta, async (req, res, next) => {
  try {
    const perfil = perfilDe(req);
    const { senha_atual, senha_nova } = trocaSenhaSchema.parse(req.body);
    await exigirSenha(perfil.id, senha_atual);
    await atualizarSenha(perfil.id, await hashSenha(senha_nova));
    res.status(204).end();
  } catch (e) {
    next(e);
  }
});
```

- [ ] **Step 3: Typecheck e testes**

Run (em `backend/`): `npm run typecheck && npm test`
Expected: sem erros de tipo; suíte inteira PASS.

- [ ] **Step 4: Conferir na mão com a stack rodando**

Com `docker compose up -d --build` (ou `npm run dev` no backend com Postgres local) e um token em `$T`:

```bash
curl -s -o /dev/null -w "%{http_code}\n" -X PUT localhost:3000/api/me/senha \
  -H "Authorization: Bearer $T" -H "Content-Type: application/json" \
  -d '{"senha_atual":"errada","senha_nova":"novasenha123"}'
```
Expected: `400`, e o corpo (sem `-o /dev/null`) é `{"error":{"code":"SENHA_INCORRETA","message":"senha atual incorreta"}}`.

Repetir com a senha certa → `204`; `POST /api/auth/login` com a senha nova → `200`, com a antiga → `401`.
(Porta: a de `PORTA` no `.env` do backend; atrás do nginx do compose use a URL pública.)

- [ ] **Step 5: Commit**

```bash
git add backend/src/repos/usuarios.ts backend/src/routes/me.ts
git commit -m "PUT /api/me/senha: troca a senha conferindo a atual, com limite por usuário"
```

---

### Task 4: `DELETE /api/me`

**Files:**
- Modify: `backend/src/repos/usuarios.ts` (fim do arquivo)
- Modify: `backend/src/routes/me.ts` (imports e fim do arquivo)
- Modify: `docs/deploy.md` (seção `## Backup`, depois do parágrafo "Copie os dumps para fora da VPS…")

**Interfaces:**
- Consumes: `limiteConta`, `exigirSenha` (Task 3); `caminhoDaMidia` (Task 2).
- Produces: `apagarUsuario(id: string): Promise<string[]>` — devolve as `midia_url` que existiam.

- [ ] **Step 1: Repositório**

No fim de `backend/src/repos/usuarios.ts`:

```ts
/**
 * Apaga a conta e devolve as `midia_url` dos registros dela, lidas na mesma transação,
 * para a rota limpar o disco só depois do COMMIT (arquivo apagado com banco revertido
 * deixaria registro apontando pro nada). O resto sai pelos ON DELETE CASCADE.
 */
export async function apagarUsuario(id: string): Promise<string[]> {
  const cliente = await pool.connect();
  try {
    await cliente.query('BEGIN');
    const midias = await cliente.query<{ midia_url: string }>(
      `SELECT midia_url FROM registros_alimentares
       WHERE user_id = $1 AND midia_url IS NOT NULL`,
      [id],
    );
    await cliente.query('DELETE FROM users WHERE id = $1', [id]);
    await cliente.query('COMMIT');
    return midias.rows.map((l) => l.midia_url);
  } catch (e) {
    await cliente.query('ROLLBACK');
    throw e;
  } finally {
    cliente.release();
  }
}
```

- [ ] **Step 2: Rota**

Em `backend/src/routes/me.ts`, adicionar ao topo:

```ts
import { unlink } from 'node:fs/promises';
import { caminhoDaMidia } from '../lib/uploads.js';
```

e incluir `apagarUsuario` no import de `../repos/usuarios.js` (ordem alfabética: `apagarUsuario, atualizarSenha, atualizarUsuario, buscarPorId, paraPerfil, type CamposAtualizaveis`). No fim do arquivo:

```ts
const exclusaoSchema = z.object({
  senha: z.string().min(1, 'informe a senha').max(200),
});

rotasMe.delete('/', limiteConta, async (req, res, next) => {
  try {
    const perfil = perfilDe(req);
    const { senha } = exclusaoSchema.parse(req.body);
    await exigirSenha(perfil.id, senha);

    const midias = await apagarUsuario(perfil.id);
    for (const url of midias) {
      const caminho = caminhoDaMidia(url);
      if (!caminho) continue;
      // ENOENT é normal: a limpeza de retenção já pode ter levado o arquivo.
      await unlink(caminho).catch((erro: NodeJS.ErrnoException) => {
        if (erro.code !== 'ENOENT') console.error('[excluir-conta] não apaguei', caminho, erro);
      });
    }
    res.status(204).end();
  } catch (e) {
    next(e);
  }
});
```

- [ ] **Step 3: Nota no deploy**

Em `docs/deploy.md`, na seção `## Backup`, depois do parágrafo "Copie os dumps para fora da VPS de vez em quando — backup que mora no mesmo disco não é backup.", inserir:

```md
Excluir a conta pelo app (`perfil › excluir conta`) apaga tudo do banco na hora e as fotos/áudios
do disco. O único caminho de volta é restaurar um dump anterior (abaixo) — a conta volta, as mídias não.
```

- [ ] **Step 4: Typecheck e testes**

Run (em `backend/`): `npm run typecheck && npm test`
Expected: sem erros; suíte PASS.

- [ ] **Step 5: Conferir na mão (conta descartável)**

Com `PERMITIR_CADASTRO` habilitado no ambiente de dev, criar conta de teste, registrar uma refeição por **foto** e anotar a `midia_url`. Depois, com o token `$T` dessa conta:

```bash
curl -s -X DELETE localhost:3000/api/me -H "Authorization: Bearer $T" \
  -H "Content-Type: application/json" -d '{"senha":"errada"}'
```
Expected: `{"error":{"code":"SENHA_INCORRETA","message":"senha atual incorreta"}}` (status 400).

Com a senha certa → `204`. Em seguida: `GET /api/me` com o mesmo token → `401`; o arquivo de `midia_url` não existe mais em `DIR_UPLOADS`; `docker compose exec -T postgres psql -U dieta dieta -c "select count(*) from registros_alimentares where user_id = '<id>'"` → `0`.

- [ ] **Step 6: Commit**

```bash
git add backend/src/repos/usuarios.ts backend/src/routes/me.ts docs/deploy.md
git commit -m "DELETE /api/me: apaga a conta com senha e limpa as mídias depois do commit"
```

---

### Task 5: Tela `trocar senha` (`/perfil/senha`)

**Files:**
- Create: `frontend/src/lib/senha.ts`
- Create: `frontend/src/lib/senha.test.ts`
- Modify: `frontend/src/lib/api.ts` (objeto `api`, depois de `salvarPerfil`)
- Create: `frontend/src/pages/perfil/SenhaPage.tsx`
- Modify: `frontend/src/App.tsx` (rota ao lado de `/perfil/metas`, `/perfil/dados`, `/perfil/refeicoes`)

**Interfaces:**
- Consumes: `api`, `mensagemDoErro` (`lib/api.ts`); `SubTela` (`components/perfil/SubTela.tsx`), `BotaoCta` (`components/ui/BotaoCta.tsx`), `Erro` (`components/Erro.tsx`) do plano B; `Protegida` em `App.tsx`.
- Produces: `validarNovaSenha(nova: string, confirmacao: string): string | null`; `api.trocarSenha(senha_atual: string, senha_nova: string): Promise<void>`; `api.excluirConta(senha: string): Promise<void>`.

- [ ] **Step 1: Teste que falha**

`frontend/src/lib/senha.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { validarNovaSenha } from './senha';

describe('validarNovaSenha', () => {
  it('aceita senha de 8+ caracteres confirmada igual', () => {
    expect(validarNovaSenha('abcdefgh', 'abcdefgh')).toBeNull();
  });

  it('recusa senha curta antes de olhar a confirmação', () => {
    expect(validarNovaSenha('abc', 'xyz')).toBe('a senha nova precisa de pelo menos 8 caracteres');
  });

  it('recusa senha longa demais', () => {
    const longa = 'a'.repeat(201);
    expect(validarNovaSenha(longa, longa)).toBe('a senha nova pode ter no máximo 200 caracteres');
  });

  it('recusa confirmação diferente', () => {
    expect(validarNovaSenha('abcdefgh', 'abcdefgi')).toBe('as senhas não batem');
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run (em `frontend/`): `npx vitest run src/lib/senha.test.ts`
Expected: FAIL — `Failed to resolve import "./senha"`.

- [ ] **Step 3: Implementar a validação e o cliente**

`frontend/src/lib/senha.ts`:

```ts
/** Mesmas regras do backend (8–200), checadas antes de gastar uma tentativa do limite. */
export function validarNovaSenha(nova: string, confirmacao: string): string | null {
  if (nova.length < 8) return 'a senha nova precisa de pelo menos 8 caracteres';
  if (nova.length > 200) return 'a senha nova pode ter no máximo 200 caracteres';
  if (nova !== confirmacao) return 'as senhas não batem';
  return null;
}
```

Em `frontend/src/lib/api.ts`, dentro de `export const api = { ... }`, logo depois de `salvarPerfil`:

```ts
  trocarSenha: (senha_atual: string, senha_nova: string) =>
    requisitar<void>('/me/senha', { method: 'PUT', corpo: { senha_atual, senha_nova } }),

  excluirConta: (senha: string) => requisitar<void>('/me', { method: 'DELETE', corpo: { senha } }),
```

(Senha errada volta 400 `SENHA_INCORRETA`, então o auto-logout de 401 em `requisitar` não dispara.)

- [ ] **Step 4: Rodar o teste**

Run (em `frontend/`): `npx vitest run src/lib/senha.test.ts`
Expected: 4 testes PASS.

- [ ] **Step 5: Página**

Mesma casca das irmãs `MetasPage`/`DadosPage`: `SubTela` com `voltarPara="/perfil"` e o CTA no `rodape`. O `rodape` fica fora do `<form>` (a `SubTela` o renderiza depois do conteúdo), então o botão aponta para o formulário pelo atributo `form` — assim Enter num campo também envia.

`frontend/src/pages/perfil/SenhaPage.tsx`:

```tsx
import { useState } from 'react';
import type { FormEvent } from 'react';
import Box from '@mui/material/Box';
import TextField from '@mui/material/TextField';
import { useNavigate } from 'react-router-dom';
import { api, mensagemDoErro } from '../../lib/api';
import { validarNovaSenha } from '../../lib/senha';
import SubTela from '../../components/perfil/SubTela';
import BotaoCta from '../../components/ui/BotaoCta';
import Erro from '../../components/Erro';

const ID_FORM = 'form-trocar-senha';
const limite = { htmlInput: { maxLength: 200 } };

export default function SenhaPage() {
  const navigate = useNavigate();
  const [atual, setAtual] = useState('');
  const [nova, setNova] = useState('');
  const [confirmacao, setConfirmacao] = useState('');
  const [erro, setErro] = useState<string | null>(null);
  const [salvando, setSalvando] = useState(false);

  async function salvar(evento: FormEvent) {
    evento.preventDefault();
    if (atual === '') return setErro('informe a senha atual');
    const invalida = validarNovaSenha(nova, confirmacao);
    if (invalida !== null) return setErro(invalida);

    setSalvando(true);
    setErro(null);
    try {
      await api.trocarSenha(atual, nova);
      navigate('/perfil', { replace: true });
    } catch (falha: unknown) {
      setErro(mensagemDoErro(falha));
      setSalvando(false);
    }
  }

  return (
    <SubTela
      titulo="trocar senha"
      voltarPara="/perfil"
      rodape={
        <BotaoCta type="submit" form={ID_FORM} disabled={salvando}>
          {salvando ? 'salvando...' : 'salvar senha'}
        </BotaoCta>
      }
    >
      {erro !== null && <Erro mensagem={erro} aoFechar={() => setErro(null)} />}
      <Box
        component="form"
        id={ID_FORM}
        onSubmit={(evento: FormEvent) => void salvar(evento)}
        sx={{ display: 'flex', flexDirection: 'column', gap: '12px' }}
      >
        <TextField
          label="senha atual"
          type="password"
          autoFocus
          autoComplete="current-password"
          value={atual}
          onChange={(e) => setAtual(e.target.value)}
          slotProps={limite}
        />
        <TextField
          label="senha nova"
          type="password"
          autoComplete="new-password"
          value={nova}
          onChange={(e) => setNova(e.target.value)}
          helperText="pelo menos 8 caracteres"
          slotProps={limite}
        />
        <TextField
          label="confirmar senha nova"
          type="password"
          autoComplete="new-password"
          value={confirmacao}
          onChange={(e) => setConfirmacao(e.target.value)}
          slotProps={limite}
        />
      </Box>
    </SubTela>
  );
}
```

(`BotaoCta` espalha as props no `ButtonBase`, que renderiza `<button>` com os atributos HTML, então `type` e `form` chegam no elemento. Se o typecheck do MUI 9 recusar `form`, trocar `type`/`form` por `onClick={() => (document.getElementById(ID_FORM) as HTMLFormElement | null)?.requestSubmit()}`.)

- [ ] **Step 6: Rota**

Em `frontend/src/App.tsx`, junto dos imports das subtelas do plano B:

```tsx
import SenhaPage from './pages/perfil/SenhaPage';
```

e logo depois de `<Route path="/perfil/refeicoes" … />` (irmã da `Casca`, sem tab bar):

```tsx
<Route path="/perfil/senha" element={<Protegida><SenhaPage /></Protegida>} />
```

- [ ] **Step 7: Verificar**

Run (em `frontend/`): `npm test && npm run typecheck && npm run build`
Expected: testes PASS, sem erros de tipo, build ok.

- [ ] **Step 8: Commit**

```bash
git add frontend/src/lib/senha.ts frontend/src/lib/senha.test.ts frontend/src/lib/api.ts frontend/src/pages/perfil/SenhaPage.tsx frontend/src/App.tsx
git commit -m "Subtela de trocar senha no perfil"
```

---

### Task 6: Linhas da CONTA e diálogo de excluir conta

**Files:**
- Create: `frontend/src/components/perfil/DialogoExcluirConta.tsx`
- Modify: `frontend/src/pages/Perfil.tsx` (chamada de `SecaoConta`)

**Interfaces:**
- Consumes: `api.excluirConta(senha)` (Task 5); `useAuth().sair()`; `SecaoConta`, `LinhaLista`, `BotaoCta`, `Erro` (plano B).
- Produces: `DialogoExcluirConta({ aberto: boolean; aoFechar: () => void })` (default export).

- [ ] **Step 1: Diálogo**

Mesmo `Paper` e mesmos botões do `useConfirmacao` do plano B (cancelar em `contorno` com foco, ação em `primario` pintado de `error.main`), mais o campo de senha. `useConfirmacao` não serve direto porque não aceita campo.

`frontend/src/components/perfil/DialogoExcluirConta.tsx`:

```tsx
import { useState } from 'react';
import type { FormEvent } from 'react';
import Box from '@mui/material/Box';
import Dialog from '@mui/material/Dialog';
import TextField from '@mui/material/TextField';
import Typography from '@mui/material/Typography';
import { useNavigate } from 'react-router-dom';
import { useAuth } from '../../auth/useAuth';
import { api, mensagemDoErro } from '../../lib/api';
import Erro from '../Erro';
import BotaoCta from '../ui/BotaoCta';

interface Props {
  aberto: boolean;
  aoFechar: () => void;
}

export default function DialogoExcluirConta({ aberto, aoFechar }: Props) {
  const { sair } = useAuth();
  const navigate = useNavigate();
  const [senha, setSenha] = useState('');
  const [erro, setErro] = useState<string | null>(null);
  const [excluindo, setExcluindo] = useState(false);

  function fechar() {
    if (excluindo) return;
    setSenha('');
    setErro(null);
    aoFechar();
  }

  async function excluir(evento: FormEvent) {
    evento.preventDefault();
    if (senha === '') return setErro('informe a senha');
    setExcluindo(true);
    setErro(null);
    try {
      await api.excluirConta(senha);
      sair();
      navigate('/login', { replace: true });
    } catch (falha: unknown) {
      setErro(mensagemDoErro(falha));
      setExcluindo(false);
    }
  }

  return (
    <Dialog
      open={aberto}
      onClose={fechar}
      slotProps={{
        paper: { sx: { borderRadius: '20px', p: '22px', m: '22px', bgcolor: 'background.default', backgroundImage: 'none' } },
      }}
    >
      <Box
        component="form"
        onSubmit={(evento: FormEvent) => void excluir(evento)}
        sx={{ display: 'flex', flexDirection: 'column', gap: '14px' }}
      >
        <Typography sx={{ fontWeight: 700, fontSize: 20, letterSpacing: '-.02em' }}>excluir conta</Typography>
        <Typography sx={{ fontSize: 13.5, lineHeight: 1.45, color: 'text.secondary' }}>
          apaga de vez seus registros, fotos, água, amizades, comentários e os grupos que você criou. não tem
          como desfazer.
        </Typography>
        {erro !== null && <Erro mensagem={erro} aoFechar={() => setErro(null)} />}
        <TextField
          label="sua senha"
          type="password"
          autoComplete="current-password"
          value={senha}
          onChange={(e) => setSenha(e.target.value)}
          slotProps={{ htmlInput: { maxLength: 200 } }}
        />
        <Box sx={{ display: 'flex', gap: '9px' }}>
          {/* Cancelar vem primeiro e leva o foco: Enter afobado não apaga nada. */}
          <BotaoCta variante="contorno" autoFocus disabled={excluindo} onClick={fechar} sx={{ flex: 1 }}>
            cancelar
          </BotaoCta>
          <BotaoCta
            type="submit"
            variante="primario"
            disabled={excluindo}
            sx={{ flex: 1, bgcolor: 'error.main', '&:hover': { bgcolor: 'error.main', filter: 'brightness(.92)' } }}
          >
            {excluindo ? 'excluindo...' : 'excluir de vez'}
          </BotaoCta>
        </Box>
      </Box>
    </Dialog>
  );
}
```

- [ ] **Step 2: Linhas no perfil via `SecaoConta`**

Em `frontend/src/pages/Perfil.tsx` (versão do plano B, Task 9):

1. Import novo (o `useState` e o `useNavigate` já estão importados; `LinhaLista` e `SecaoConta` também):

```tsx
import DialogoExcluirConta from '../components/perfil/DialogoExcluirConta';
```

2. Junto dos outros estados, **antes** do `if (perfil === null) return null;` (regra dos hooks), logo depois de `const [salvandoModo, setSalvandoModo] = useState(false);`:

```tsx
  const [excluindoConta, setExcluindoConta] = useState(false);
```

3. Trocar a última linha de dentro de `<Tela gap={26}>`, que hoje é `<SecaoConta />`, por:

```tsx
      <SecaoConta
        antesDeSair={<LinhaLista rotulo="trocar senha" chevron onClick={() => navigate('/perfil/senha')} />}
        depoisDeSair={
          <LinhaLista rotulo="excluir conta" cor="perigo" ultima onClick={() => setExcluindoConta(true)} />
        }
      />
      <DialogoExcluirConta aberto={excluindoConta} aoFechar={() => setExcluindoConta(false)} />
```

Com `depoisDeSair` preenchido, a `SecaoConta` já tira o `ultima` de `sair`.

- [ ] **Step 3: Verificar**

Run (em `frontend/`): `npm test && npm run typecheck && npm run build`
Expected: tudo verde.

- [ ] **Step 4: Conferir no navegador**

`docker compose up -d --build` (ou `npm run dev` no backend e no frontend), entrar com uma conta de teste e, no celular (ou DevTools em 390×844), claro **e** escuro:
- `perfil` mostra, em CONTA: `trocar senha ›`, `sair`, `excluir conta` (as duas últimas em `error.main`), com o espaço extra embaixo só em `excluir conta`.
- `trocar senha` abre sem tab bar, com `← voltar` e título grande como `metas`; senha atual errada mostra "senha atual incorreta" e **continua logado**; senhas diferentes mostram "as senhas não batem" sem chamar a API; Enter no último campo envia; com tudo certo volta ao perfil e o login novo usa a senha nova.
- `excluir conta` abre o diálogo com a mesma cara do "sair do grupo"; cancelar fecha sem apagar e limpa a senha; senha errada mostra o erro e continua logado; senha certa cai no `/login` e o login antigo passa a dar "e-mail ou senha incorretos".

- [ ] **Step 5: Commit**

```bash
git add frontend/src/components/perfil/DialogoExcluirConta.tsx frontend/src/pages/Perfil.tsx
git commit -m "Linhas de trocar senha e excluir conta no perfil, com diálogo de confirmação"
```

---

### Task 7: Revisão final

**Files:** nenhum novo.

- [ ] **Step 1: Suítes completas**

Run (em `backend/`): `npm test && npm run typecheck && npm run build`
Run (em `frontend/`): `npm test && npm run typecheck && npm run build`
Expected: tudo verde.

- [ ] **Step 2: Contrato x código**

Conferir que `docs/api-contract.md` bate com o código: caminhos (`/api/me/senha`, `/api/me`), campos (`senha_atual`, `senha_nova`, `senha`), limites (8–200; 10 por 15 min), códigos (`SENHA_INCORRETA` 400, `LIMITE_EXCEDIDO` 429) e mensagem ("senha atual incorreta").

- [ ] **Step 3: Frontend usa só as peças do plano B**

Run (em `frontend/`): `git grep -n "className\|ListItemButton\|InputBase" -- src/pages/perfil/SenhaPage.tsx src/components/perfil/DialogoExcluirConta.tsx src/pages/Perfil.tsx`
Expected: sem resultado (nada de CSS puro nem campo sublinhado avulso; campos são `TextField`, linhas são `LinhaLista`, botões são `BotaoCta`).

Run: `git grep -n "perfil/senha" -- src/App.tsx src/pages/Perfil.tsx`
Expected: uma rota `<Route path="/perfil/senha" element={<Protegida><SenhaPage /></Protegida>} />` irmã de `/perfil/refeicoes` (fora da `Casca`) e um `navigate('/perfil/senha')` no `Perfil.tsx`.

- [ ] **Step 4: Revisão de segurança**

Pela tabela de roteamento do CLAUDE.md, este plano toca auth e segredos: rodar o `security-reviewer` (ou `/security-review`) sobre o diff dos commits deste plano antes do merge, conferindo que a senha nunca é logada, que a exclusão só apaga arquivos resolvidos por `caminhoDaMidia` e que o limite vale para os dois endpoints.
