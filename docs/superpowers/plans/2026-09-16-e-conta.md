# Conta: trocar senha e desativar conta — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Modelo:** cada tarefa é implementada por subagente **Sonnet** (`model: "sonnet"`); a sessão Opus só coordena e revisa. Não executar antes de o usuário liberar.

**Goal:** Dar à pessoa um jeito de trocar a senha e de **desativar** a conta (nada é apagado) a partir da seção CONTA do perfil; conta desativada não entra mais, some do social e só volta pelo administrador, com um script na VPS.

**Architecture:** Dois endpoints novos em `backend/src/routes/me.ts` (`PUT /api/me/senha`, `POST /api/me/desativar`) com SQL em `repos/usuarios.ts`, códigos de erro `SENHA_INCORRETA` (400) e `CONTA_DESATIVADA` (403) e limite de tentativas por usuário. A migration `005` cria `users.desativada_em`; login e middleware de autenticação barram a conta; as consultas sociais (planos atuais + plano D) filtram `desativada_em IS NULL`. Reativação: `backend/src/scripts/reativarConta.ts`. Frontend: subtela `/perfil/senha`, diálogo `desativar conta` e tratamento de `CONTA_DESATIVADA` como logout com a mensagem aparecendo no login.

**Tech Stack:** Express 5 + Zod 4 + `pg` + bcryptjs (backend, Vitest); React 19 + MUI 9 + react-router 7 (frontend, Vitest em ambiente `node`).

**Spec:** `docs/superpowers/specs/2026-09-16-redesign-liquid-glass-design.md` (decisão D14; seções "Conta", "Telas › Perfil", "Casca do app e rotas").

**Interfaces que vêm dos planos anteriores (já executados):**
- Plano B (`docs/superpowers/plans/2026-09-16-b-telas.md`, Tasks 2, 3, 10 e 11):
  - `frontend/src/App.tsx` registra as subtelas do perfil como rotas protegidas **irmãs** da `Casca` (sem tab bar): `<Route path="/perfil/metas" element={<Protegida><MetasPage /></Protegida>} />`. Páginas em `frontend/src/pages/perfil/` com sufixo `Page`.
  - `SubTela({ titulo: string; voltarPara: string; children: ReactNode; rodape?: ReactNode })` em `frontend/src/components/perfil/SubTela.tsx`.
  - `SecaoConta({ antesDeSair?: ReactNode; depoisDeSair?: ReactNode })` em `frontend/src/components/perfil/SecaoConta.tsx`; `pages/Perfil.tsx` renderiza `<SecaoConta />` como último filho de `<Tela gap={26}>`. `sair` só é `ultima` quando `depoisDeSair` não vem.
  - `LinhaLista({ rotulo: ReactNode; valor?: ReactNode; chevron?: boolean; cor?: 'normal' | 'perigo'; onClick?: () => void; ultima?: boolean; children?: ReactNode })`, `BotaoCta(ButtonBaseProps & { variante?: 'primario' | 'contorno' | 'perigo'; altura?: number })` em `components/ui/`; `Erro({ mensagem, aoFechar? })` em `components/Erro.tsx`.
  - `useConfirmacao()` só tem título/texto/rótulo — sem campo de senha. O diálogo daqui usa o **mesmo** `Paper` (`borderRadius 20px`, `p 22px`, `m 22px`, `bgcolor background.default`, `backgroundImage none`) e os mesmos `BotaoCta`.
  - `pages/Login.tsx` (Task 3) guarda `const [erro, setErro] = useState<string | null>(null)` e mostra `<Erro mensagem={erro} …/>` no topo do formulário. A rota `/login` monta `Login` também enquanto `carregando` é `true`.
  - `Perfil` não tem aviso vindo de navegação; trocar a senha só volta para `/perfil`.
- Plano D (`docs/superpowers/plans/2026-09-16-d-social.md`, Tasks 4–6): `idsVisiveis(userId)` em `repos/social.ts`; `feedDeUsuarios(observadorId, userIds, antes, limite)` em `repos/registros.ts` com `JOIN users u ON u.id = r.user_id`; `repos/interacoes.ts` com `donoDoRegistro(registroId)` e `SELECT_COMENTARIO` (`JOIN users u ON u.id = cm.user_id`) usado por `listarComentarios`; `minhaPosicaoSemana` usa `membrosDoGrupo`.
- Frontend já tem `npm test` (`vitest run`, ambiente `node`) — plano A.

## Global Constraints

- **Este plano roda depois do plano D**: a migration `005` pressupõe a `004`, e a Task 4 edita consultas criadas no D.
- Contrato primeiro: `docs/api-contract.md` é atualizado antes do código; divergência é bug.
- `PUT /api/me/senha` body `{ senha_atual: string, senha_nova: string (8–200) }` → `204`; `400 SENHA_INCORRETA` ("senha atual incorreta") — **não** 401, que derruba a sessão no frontend.
- `POST /api/me/desativar` body `{ senha: string }` → `204`; `400 SENHA_INCORRETA`. **Nada é apagado.**
- Conta desativada: login → `403 CONTA_DESATIVADA` ("conta desativada — fale com o administrador"), checado **depois** de conferir a senha; tokens dela → mesmo `403`; some de amigos, pedidos, membros de grupo (e contagem de membros), feeds, comentários, ranking, busca `nome#tag` (`404`) e perfil público (`404`). Grupos que ela criou continuam.
- Reativação só na VPS: `npm run conta:reativar -- <email>` (dev) / `node dist/scripts/reativarConta.js <email>` (container). Sem tela nem endpoint de admin.
- Limite: 10 tentativas / 15 min por usuário, somando os dois endpoints.
- Toda entrada validada com Zod antes de tocar no banco; erros via `AppError`; SQL puro em `repos/`; migration nova, nunca editar as antigas.
- Testes Vitest só de lógica pura, sem rede e sem banco.
- Frontend sem CSS puro: MUI (`sx`/`styled`) e tokens do tema. Campos `TextField`, CTA `BotaoCta`, linhas `LinhaLista` (plano B).
- Textos da interface em minúsculas, português, no tom do app.
- Commits em português, terminando com as linhas de atribuição do repositório.

---

### Task 1: Contrato + códigos `SENHA_INCORRETA` e `CONTA_DESATIVADA`

**Files:**
- Modify: `docs/api-contract.md:14-16` (lista de códigos), `:94-95` (login), depois do bloco `### PUT /api/me` (linha ~104) e seção `## Rede social` (parágrafo "Quem vê o quê")
- Modify: `backend/src/lib/erros.ts`

**Interfaces:**
- Produces: `CodigoErro` inclui `'SENHA_INCORRETA'` (400) e `'CONTA_DESATIVADA'` (403).

- [ ] **Step 1: Lista de códigos**

Em `docs/api-contract.md`, trocar:

```md
  `NAO_ENCONTRADO`, `IA_INDISPONIVEL`, `IA_RESPOSTA_INVALIDA`, `ARQUIVO_INVALIDO`, `ERRO_INTERNO`,
  `SEM_ACESSO`, `PEDIDO_DUPLICADO`, `REFEICAO_EM_USO`.
```

por:

```md
  `NAO_ENCONTRADO`, `IA_INDISPONIVEL`, `IA_RESPOSTA_INVALIDA`, `ARQUIVO_INVALIDO`, `ERRO_INTERNO`,
  `SEM_ACESSO`, `PEDIDO_DUPLICADO`, `REFEICAO_EM_USO`, `SENHA_INCORRETA`, `LIMITE_EXCEDIDO`,
  `CONTA_DESATIVADA`.
- Conta desativada: **qualquer** rota autenticada responde `403 CONTA_DESATIVADA`; o cliente trata
  como fim de sessão (igual a 401) e mostra a mensagem no login.
```

- [ ] **Step 2: Login**

Trocar:

```md
### `POST /api/auth/login`
Body: `{ email, senha }` → `200 { token, perfil }` · `401 CREDENCIAIS_INVALIDAS`
```

por:

```md
### `POST /api/auth/login`
Body: `{ email, senha }` → `200 { token, perfil }` · `401 CREDENCIAIS_INVALIDAS` ·
`403 CONTA_DESATIVADA` ("conta desativada — fale com o administrador"). O 403 só aparece para
quem acertou a senha: senha errada numa conta desativada continua `401`.
```

- [ ] **Step 3: Endpoints de conta**

Logo depois do bloco `### PUT /api/me` (termina em "→ `200 Perfil` (já com metas recalculadas se `metas_automaticas`).") e antes de `### GET /api/refeicoes`, inserir:

```md
### `PUT /api/me/senha`
Body: `{ senha_atual: string, senha_nova: string }` (`senha_nova` com 8–200 chars) → `204`
`400 SENHA_INCORRETA` (senha atual não confere) · `400 VALIDACAO` ·
`429 LIMITE_EXCEDIDO` (10 tentativas por 15 min por usuário, somando este endpoint e `POST /api/me/desativar`).
Não é 401 de propósito: 401 derruba a sessão no cliente. O token atual continua valendo.

### `POST /api/me/desativar`
Body: `{ senha: string }` → `204` · `400 SENHA_INCORRETA` · `400 VALIDACAO` · `429 LIMITE_EXCEDIDO`
Desativa a conta. **Nada é apagado**: registros, água, refeições, mídias, amizades e grupos ficam
no banco. A partir daí o login responde `403 CONTA_DESATIVADA`, o token atual também, e a pessoa
some do social (ver "Quem vê o quê"). Só o administrador do servidor reativa, pela linha de
comando (`docs/deploy.md`, "Contas desativadas").
```

- [ ] **Step 4: Invisibilidade no social**

Na seção `## Rede social`, logo depois do parágrafo que começa com "**Quem vê o quê:**", inserir:

```md
**Conta desativada some da rede:** não aparece em `GET /api/amigos`, nos pedidos, nos membros e na
`quantidade_membros` de grupos, no ranking, em nenhum feed nem nos comentários; `POST
/api/amigos/pedidos` para o `nome#tag` dela dá `404 NAO_ENCONTRADO`; `GET /api/social/usuarios/:id`
(e `/calendario`, `/refeicoes`) dá `404 NAO_ENCONTRADO`; curtir/comentar um post dela dá `404`.
Os grupos criados por ela continuam existindo. Curtidas antigas dela continuam contando no total.
```

- [ ] **Step 5: Códigos no backend**

Em `backend/src/lib/erros.ts`, no tipo `CodigoErro`, depois de `| 'CREDENCIAIS_INVALIDAS'`:

```ts
  | 'SENHA_INCORRETA'
  | 'CONTA_DESATIVADA'
```

e no mapa `STATUS_PADRAO`, depois de `CREDENCIAIS_INVALIDAS: 401,`:

```ts
  // 400, não 401: quem erra a senha atual continua logado.
  SENHA_INCORRETA: 400,
  CONTA_DESATIVADA: 403,
```

- [ ] **Step 6: Typecheck**

Run (em `backend/`): `npm run typecheck`
Expected: sem erros (o `Record<CodigoErro, number>` obriga as duas entradas no mapa).

- [ ] **Step 7: Commit**

```bash
git add docs/api-contract.md backend/src/lib/erros.ts
git commit -m "Contrato de trocar senha e desativar conta, com SENHA_INCORRETA e CONTA_DESATIVADA"
```

---

### Task 2: `PUT /api/me/senha`

**Files:**
- Modify: `backend/src/repos/usuarios.ts` (fim do arquivo)
- Modify: `backend/src/routes/me.ts`

**Interfaces:**
- Consumes: `SENHA_INCORRETA` (Task 1); `buscarPorId(id)`, `conferirSenha(senha, hash)`, `hashSenha(senha)`; `limiteTaxa`, `porUsuario`.
- Produces: `atualizarSenha(id: string, passwordHash: string): Promise<void>`; em `routes/me.ts`, `limiteConta` (middleware) e `exigirSenha(userId: string, senha: string): Promise<void>` — reusados na Task 3.

- [ ] **Step 1: Repositório**

No fim de `backend/src/repos/usuarios.ts`:

```ts
export async function atualizarSenha(id: string, passwordHash: string): Promise<void> {
  await consultar('UPDATE users SET password_hash = $2 WHERE id = $1', [id, passwordHash]);
}
```

- [ ] **Step 2: Rota**

Em `backend/src/routes/me.ts`, trocar os imports do topo por:

```ts
import { Router } from 'express';
import { z } from 'zod';
import { perfilDe } from '../middleware/autenticar.js';
import { limiteTaxa, porUsuario } from '../middleware/limiteTaxa.js';
import { calcularMetas } from '../domain/nutricao.js';
import { timezoneValida } from '../domain/tempo.js';
import { OBJETIVOS, SEXOS } from '../domain/tipos.js';
import { conferirSenha, hashSenha } from '../lib/auth.js';
import { AppError } from '../lib/erros.js';
import {
  atualizarSenha,
  atualizarUsuario,
  buscarPorId,
  paraPerfil,
  type CamposAtualizaveis,
} from '../repos/usuarios.js';
```

E no fim do arquivo:

```ts
// Com um token vazado dá para chutar a senha atual por aqui: limita por usuário.
const limiteConta = limiteTaxa({
  janelaMs: 15 * 60 * 1000,
  maximo: 10,
  chave: porUsuario,
  mensagem: 'muitas tentativas com a senha, aguarde alguns minutos',
});

async function exigirSenha(userId: string, senha: string): Promise<void> {
  const linha = await buscarPorId(userId);
  const ok = linha ? await conferirSenha(senha, linha.password_hash) : false;
  if (!ok) throw new AppError('SENHA_INCORRETA', 'senha atual incorreta');
}

const trocaSenhaSchema = z.object({
  senha_atual: z.string().min(1, 'informe a senha atual').max(200),
  senha_nova: z
    .string()
    .min(8, 'a senha nova precisa de pelo menos 8 caracteres')
    .max(200, 'a senha nova pode ter no máximo 200 caracteres'),
});

rotasMe.put('/senha', limiteConta, async (req, res, next) => {
  try {
    const perfil = perfilDe(req);
    const { senha_atual, senha_nova } = trocaSenhaSchema.parse(req.body);
    await exigirSenha(perfil.id, senha_atual);
    await atualizarSenha(perfil.id, await hashSenha(senha_nova));
    res.status(204).end();
  } catch (e) {
    next(e);
  }
});
```

- [ ] **Step 3: Typecheck e testes**

Run (em `backend/`): `npm run typecheck && npm test`
Expected: sem erros de tipo; suíte inteira PASS.

- [ ] **Step 4: Conferir na mão com a stack rodando**

Com `npm run dev` no backend (Postgres local) e um token em `$T`:

```bash
curl -s -w "\n%{http_code}\n" -X PUT localhost:3001/api/me/senha \
  -H "Authorization: Bearer $T" -H "Content-Type: application/json" \
  -d '{"senha_atual":"errada","senha_nova":"novasenha123"}'
```
Expected: `{"error":{"code":"SENHA_INCORRETA","message":"senha atual incorreta"}}` e `400`.

Repetir com a senha certa → `204`; `POST /api/auth/login` com a senha nova → `200`, com a antiga → `401`.
(Porta: a de `PORTA` no `.env` do backend.)

- [ ] **Step 5: Commit**

```bash
git add backend/src/repos/usuarios.ts backend/src/routes/me.ts
git commit -m "PUT /api/me/senha: troca a senha conferindo a atual, com limite por usuário"
```

---

### Task 3: Migration 005 + `POST /api/me/desativar` + bloqueio no login e no middleware

**Files:**
- Create: `backend/src/db/migrations/005_conta_desativada.sql`
- Modify: `backend/src/repos/usuarios.ts` (`LinhaUsuario`, `COLUNAS`, fim do arquivo)
- Modify: `backend/src/routes/me.ts` (import e fim do arquivo)
- Modify: `backend/src/routes/auth.ts` (rota `/login`)
- Modify: `backend/src/middleware/autenticar.ts`

**Interfaces:**
- Consumes: `limiteConta`, `exigirSenha` (Task 2); `CONTA_DESATIVADA` (Task 1).
- Produces: `LinhaUsuario.desativada_em: Date | null`; `desativarUsuario(id: string): Promise<void>`; `MENSAGEM_CONTA_DESATIVADA` exportada de `backend/src/lib/erros.ts`.

- [ ] **Step 1: Migration**

`backend/src/db/migrations/005_conta_desativada.sql`:

```sql
-- Conta não é apagada: fica desativada. NULL = ativa. Só o administrador volta para NULL.
ALTER TABLE users ADD COLUMN IF NOT EXISTS desativada_em TIMESTAMPTZ;
```

- [ ] **Step 2: Mensagem única**

No fim de `backend/src/lib/erros.ts`:

```ts
export const MENSAGEM_CONTA_DESATIVADA = 'conta desativada — fale com o administrador';
```

- [ ] **Step 3: Repositório**

Em `backend/src/repos/usuarios.ts`:

1. Em `interface LinhaUsuario`, depois de `created_at: Date;`:

```ts
  desativada_em: Date | null;
```

2. Trocar `COLUNAS` por:

```ts
const COLUNAS = `id, email, password_hash, nome, tag, sexo, idade, peso_kg, altura_cm, objetivo,
  meta_calorias, meta_carboidrato_g, meta_proteina_g, meta_gordura_g, meta_agua_ml,
  metas_automaticas, modo_preguicoso, timezone, created_at, desativada_em`;
```

(`paraPerfil` não muda: o campo não vai para a API.)

3. No fim do arquivo:

```ts
export async function desativarUsuario(id: string): Promise<void> {
  await consultar('UPDATE users SET desativada_em = now() WHERE id = $1 AND desativada_em IS NULL', [
    id,
  ]);
}
```

- [ ] **Step 4: Middleware barra a conta sem consulta extra**

O middleware já carrega a linha inteira com `buscarPorId`; basta olhar a coluna. Em `backend/src/middleware/autenticar.ts`, trocar o import de erros por:

```ts
import { AppError, MENSAGEM_CONTA_DESATIVADA } from '../lib/erros.js';
```

e, logo depois de `if (!linha) throw new AppError('NAO_AUTORIZADO', 'essa conta não existe mais');`:

```ts
    if (linha.desativada_em !== null) {
      throw new AppError('CONTA_DESATIVADA', MENSAGEM_CONTA_DESATIVADA);
    }
```

- [ ] **Step 5: Login barra depois da senha**

Em `backend/src/routes/auth.ts`, trocar o import de erros por:

```ts
import { AppError, MENSAGEM_CONTA_DESATIVADA } from '../lib/erros.js';
```

e, na rota `/login`, logo depois do bloco `if (!linha || !ok) { … }`:

```ts
    // Só quem acertou a senha descobre que a conta está desativada.
    if (linha.desativada_em !== null) {
      throw new AppError('CONTA_DESATIVADA', MENSAGEM_CONTA_DESATIVADA);
    }
```

(O cadastro não muda: o e-mail de uma conta desativada continua em uso e responde `409 EMAIL_EM_USO`.)

- [ ] **Step 6: Rota de desativar**

Em `backend/src/routes/me.ts`, incluir `desativarUsuario` no import de `../repos/usuarios.js` (ordem: `atualizarSenha, atualizarUsuario, buscarPorId, desativarUsuario, paraPerfil, type CamposAtualizaveis`) e, no fim do arquivo:

```ts
const desativarSchema = z.object({
  senha: z.string().min(1, 'informe a senha').max(200),
});

rotasMe.post('/desativar', limiteConta, async (req, res, next) => {
  try {
    const perfil = perfilDe(req);
    const { senha } = desativarSchema.parse(req.body);
    await exigirSenha(perfil.id, senha);
    await desativarUsuario(perfil.id);
    res.status(204).end();
  } catch (e) {
    next(e);
  }
});
```

- [ ] **Step 7: Typecheck, testes e migration**

Run (em `backend/`): `npm run typecheck && npm test && npm run migrate`
Expected: sem erros; suíte PASS; `[migrate] aplicada: 005_conta_desativada.sql`.

- [ ] **Step 8: Conferir na mão (conta descartável)**

Com cadastro aberto em dev, criar conta de teste; com o token `$T` dela:

```bash
curl -s -w "\n%{http_code}\n" -X POST localhost:3001/api/me/desativar \
  -H "Authorization: Bearer $T" -H "Content-Type: application/json" -d '{"senha":"errada"}'
```
Expected: `SENHA_INCORRETA`, `400`.

Com a senha certa → `204`. Depois:
- `GET /api/me` com o mesmo token → `403` e `{"error":{"code":"CONTA_DESATIVADA",…}}`;
- `POST /api/auth/login` com a senha certa → `403 CONTA_DESATIVADA`; com senha errada → `401 CREDENCIAIS_INVALIDAS`;
- `psql … -c "select count(*) from registros_alimentares where user_id = '<id>'"` → mesma contagem de antes (nada apagado).

- [ ] **Step 9: Commit**

```bash
git add backend/src/db/migrations/005_conta_desativada.sql backend/src/lib/erros.ts backend/src/repos/usuarios.ts backend/src/routes/me.ts backend/src/routes/auth.ts backend/src/middleware/autenticar.ts
git commit -m "Desativa a conta com senha: login e sessão passam a responder CONTA_DESATIVADA"
```

---

### Task 4: Conta desativada some do social

Todas as edições são filtros `desativada_em IS NULL` nas consultas que listam ou resolvem pessoas. O ranking (`minhaPosicaoSemana`) e o feed de grupo herdam o filtro de `membrosDoGrupo`.

**Files:**
- Modify: `backend/src/repos/social.ts` (`buscarUsuarioSocial`, `listarAmigos`, `listarPedidos`, `SELECT_GRUPO`, `membrosDoGrupo`, `idsVisiveis`, `podeVer`)
- Modify: `backend/src/repos/usuarios.ts` (`buscarPorNomeTag`)
- Modify: `backend/src/repos/registros.ts` (`feedDeUsuarios`)
- Modify: `backend/src/repos/interacoes.ts` (`donoDoRegistro`, `listarComentarios`)

**Interfaces:**
- Consumes: coluna `users.desativada_em` (Task 3); funções do plano D listadas no topo.
- Produces: nenhuma assinatura nova; `estaAtiva(userId: string): Promise<boolean>` interna a `repos/social.ts` (não exportada).

- [ ] **Step 1: `repos/social.ts`**

1. `buscarUsuarioSocial` — perfil público, calendário e refeições de uma conta desativada viram `404` (a rota `alvoVisivel` já trata `null` como `NAO_ENCONTRADO`):

```ts
export async function buscarUsuarioSocial(id: string): Promise<UsuarioSocial | null> {
  return consultarUm<UsuarioSocial>(
    `SELECT ${COLUNAS_SOCIAIS} FROM users u WHERE u.id = $1 AND u.desativada_em IS NULL`,
    [id],
  );
}
```

2. `listarAmigos` — trocar a linha `WHERE a.status = 'aceita' AND $1 IN (a.solicitante_id, a.destinatario_id)` por:

```sql
     WHERE a.status = 'aceita' AND $1 IN (a.solicitante_id, a.destinatario_id)
       AND u.desativada_em IS NULL
```

3. `listarPedidos` — nas duas consultas, trocar `WHERE a.destinatario_id = $1 AND a.status = 'pendente'` por `WHERE a.destinatario_id = $1 AND a.status = 'pendente' AND u.desativada_em IS NULL` e `WHERE a.solicitante_id = $1 AND a.status = 'pendente'` por `WHERE a.solicitante_id = $1 AND a.status = 'pendente' AND u.desativada_em IS NULL`.

4. `SELECT_GRUPO` — a contagem de membros ignora desativados:

```ts
const SELECT_GRUPO = `SELECT g.id, g.nome, g.codigo_convite, g.criador_id, g.criado_em,
    (SELECT count(*)::int FROM grupo_membros m
       JOIN users mu ON mu.id = m.user_id
      WHERE m.grupo_id = g.id AND mu.desativada_em IS NULL) AS quantidade_membros
  FROM grupos g`;
```

5. `membrosDoGrupo` — trocar `WHERE m.grupo_id = $1` por:

```sql
     WHERE m.grupo_id = $1 AND u.desativada_em IS NULL
```

6. `idsVisiveis` (criada no plano D, Task 4) — substituir a função inteira:

```ts
/** Todo mundo cujo progresso a pessoa enxerga, menos ela mesma: amigos aceitos e colegas de grupo ativos. */
export async function idsVisiveis(userId: string): Promise<string[]> {
  const linhas = await consultar<{ id: string }>(
    `SELECT v.id FROM (
       SELECT CASE WHEN a.solicitante_id = $1 THEN a.destinatario_id ELSE a.solicitante_id END AS id
       FROM amizades a
       WHERE a.status = 'aceita' AND $1 IN (a.solicitante_id, a.destinatario_id)
       UNION
       SELECT outro.user_id AS id
       FROM grupo_membros meu
       JOIN grupo_membros outro ON outro.grupo_id = meu.grupo_id AND outro.user_id <> $1
       WHERE meu.user_id = $1
     ) v
     JOIN users u ON u.id = v.id
     WHERE u.desativada_em IS NULL`,
    [userId],
  );
  return linhas.map((l) => l.id);
}
```

7. `podeVer` — ninguém enxerga conta desativada (a própria pessoa nem chega aqui: o middleware já barrou). Logo antes de `podeVer`:

```ts
async function estaAtiva(userId: string): Promise<boolean> {
  const linha = await consultarUm<{ id: string }>(
    'SELECT id FROM users WHERE id = $1 AND desativada_em IS NULL',
    [userId],
  );
  return linha !== null;
}
```

e trocar o corpo de `podeVer` por:

```ts
export async function podeVer(observadorId: string, alvoId: string): Promise<boolean> {
  if (observadorId === alvoId) return true;
  if (!(await estaAtiva(alvoId))) return false;
  if (await saoAmigos(observadorId, alvoId)) return true;
  return dividemGrupo(observadorId, alvoId);
}
```

- [ ] **Step 2: `repos/usuarios.ts` — busca por `nome#tag`**

Trocar `buscarPorNomeTag` por:

```ts
/** Só contas ativas: pedido de amizade para uma conta desativada dá 404, como se não existisse. */
export async function buscarPorNomeTag(nome: string, tag: string): Promise<LinhaUsuario | null> {
  return consultarUm<LinhaUsuario>(
    `SELECT ${COLUNAS} FROM users
     WHERE lower(nome) = lower($1) AND tag = $2 AND desativada_em IS NULL`,
    [nome, tag],
  );
}
```

(`tagLivrePara` **não** muda: a tag de uma conta desativada continua ocupada, para a identidade dela não ir parar em outra pessoa se o administrador reativar.)

- [ ] **Step 3: `repos/registros.ts` — feeds**

Em `feedDeUsuarios` (versão do plano D), trocar:

```sql
     WHERE r.user_id = ANY($1::uuid[])
```

por:

```sql
     WHERE r.user_id = ANY($1::uuid[])
       AND u.desativada_em IS NULL
```

- [ ] **Step 4: `repos/interacoes.ts` — posts e comentários**

Trocar `donoDoRegistro` por (post de conta desativada → `404` em curtir/comentar):

```ts
export async function donoDoRegistro(registroId: string): Promise<string | null> {
  const linha = await consultarUm<{ user_id: string }>(
    `SELECT r.user_id FROM registros_alimentares r
     JOIN users u ON u.id = r.user_id
     WHERE r.id = $1 AND u.desativada_em IS NULL`,
    [registroId],
  );
  return linha?.user_id ?? null;
}
```

e, em `listarComentarios`, trocar `WHERE cm.registro_id = $1 ORDER BY cm.criado_em ASC` por:

```sql
WHERE cm.registro_id = $1 AND u.desativada_em IS NULL ORDER BY cm.criado_em ASC
```

(A contagem `comentarios` do feed continua contando todos — é um número, não expõe ninguém.)

- [ ] **Step 5: Typecheck e testes**

Run (em `backend/`): `npm run typecheck && npm test`
Expected: sem erros; suíte PASS.

- [ ] **Step 6: Conferir na mão**

Duas contas de teste, **A** e **B**, amigas e no mesmo grupo (criado por B), B com um registro e um comentário no post de A. Desativar B (`POST /api/me/desativar`). Com o token de A:
- `GET /api/amigos` → B não aparece;
- `GET /api/grupos` → o grupo de B continua, `quantidade_membros` diminuiu 1; `GET /api/grupos/:id` → B fora de `membros`;
- `GET /api/social/feed` e `GET /api/grupos/:id/feed` → nenhum post de B;
- `GET /api/social/usuarios/<id de B>` → `404`;
- `POST /api/amigos/pedidos` com `nome_tag` de B → `404`;
- `GET /api/social/posts/<post de A>/comentarios` → sem o comentário de B;
- `PUT /api/social/posts/<post de B>/curtida` → `404`.

- [ ] **Step 7: Commit**

```bash
git add backend/src/repos/social.ts backend/src/repos/usuarios.ts backend/src/repos/registros.ts backend/src/repos/interacoes.ts
git commit -m "Conta desativada some de amigos, grupos, feeds, comentários e busca por nome#tag"
```

---

### Task 5: Script de reativação para o administrador

**Files:**
- Create: `backend/src/scripts/argumentos.ts`
- Create: `backend/src/scripts/argumentos.test.ts`
- Create: `backend/src/scripts/reativarConta.ts`
- Modify: `backend/src/repos/usuarios.ts` (fim do arquivo)
- Modify: `backend/package.json` (`scripts`)
- Modify: `docs/deploy.md` (seção nova antes de `## Firewall`)

**Interfaces:**
- Produces: `emailDosArgumentos(args: string[]): string | null`; `reativarPorEmail(email: string): Promise<'reativada' | 'ja_ativa' | 'nao_encontrada'>`.

- [ ] **Step 1: Teste que falha**

`backend/src/scripts/argumentos.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { emailDosArgumentos } from './argumentos.js';

describe('emailDosArgumentos', () => {
  it('pega o único argumento e normaliza', () => {
    expect(emailDosArgumentos(['  Joao@UFN.edu.br '])).toBe('joao@ufn.edu.br');
  });

  it('ignora o "--" que o npm repassa', () => {
    expect(emailDosArgumentos(['--', 'joao@ufn.edu.br'])).toBe('joao@ufn.edu.br');
  });

  it('recusa sem argumento', () => {
    expect(emailDosArgumentos([])).toBeNull();
  });

  it('recusa mais de um e-mail', () => {
    expect(emailDosArgumentos(['a@b.com', 'c@d.com'])).toBeNull();
  });

  it('recusa o que não parece e-mail', () => {
    expect(emailDosArgumentos(['joao'])).toBeNull();
    expect(emailDosArgumentos(['joao@'])).toBeNull();
    expect(emailDosArgumentos(['@ufn.edu.br'])).toBeNull();
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run (em `backend/`): `npx vitest run src/scripts/argumentos.test.ts`
Expected: FAIL — `Failed to resolve import "./argumentos.js"`.

- [ ] **Step 3: Implementar a leitura do argumento**

`backend/src/scripts/argumentos.ts`:

```ts
/** E-mail passado na linha de comando, normalizado; null se faltar, sobrar ou vier torto. */
export function emailDosArgumentos(args: string[]): string | null {
  const valores = args.filter((a) => a !== '--').map((a) => a.trim().toLowerCase());
  if (valores.length !== 1) return null;
  const email = valores[0]!;
  return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email) ? email : null;
}
```

- [ ] **Step 4: Rodar o teste**

Run (em `backend/`): `npx vitest run src/scripts/argumentos.test.ts`
Expected: 5 testes PASS.

- [ ] **Step 5: Repositório**

No fim de `backend/src/repos/usuarios.ts`:

```ts
export async function reativarPorEmail(
  email: string,
): Promise<'reativada' | 'ja_ativa' | 'nao_encontrada'> {
  const linha = await consultarUm<{ desativada_em: Date | null }>(
    'SELECT desativada_em FROM users WHERE lower(email) = lower($1)',
    [email],
  );
  if (!linha) return 'nao_encontrada';
  if (linha.desativada_em === null) return 'ja_ativa';
  await consultar('UPDATE users SET desativada_em = NULL WHERE lower(email) = lower($1)', [email]);
  return 'reativada';
}
```

- [ ] **Step 6: Script**

`backend/src/scripts/reativarConta.ts` (mesmo formato de encerramento do `db/migrate.ts`):

```ts
import { pool } from '../db/index.js';
import { reativarPorEmail } from '../repos/usuarios.js';
import { emailDosArgumentos } from './argumentos.js';

const MENSAGENS = {
  reativada: 'conta reativada — a pessoa já pode entrar de novo',
  ja_ativa: 'essa conta já está ativa, nada mudou',
  nao_encontrada: 'nenhuma conta com esse e-mail',
} as const;

async function principal(): Promise<number> {
  const email = emailDosArgumentos(process.argv.slice(2));
  if (email === null) {
    console.error('uso: npm run conta:reativar -- <email>');
    return 2;
  }
  const resultado = await reativarPorEmail(email);
  console.log(`[reativar] ${email}: ${MENSAGENS[resultado]}`);
  return resultado === 'nao_encontrada' ? 1 : 0;
}

principal()
  .then(async (codigo) => {
    await pool.end();
    process.exit(codigo);
  })
  .catch(async (e: unknown) => {
    console.error('[reativar]', e);
    await pool.end();
    process.exit(1);
  });
```

- [ ] **Step 7: Scripts npm**

Em `backend/package.json`, dentro de `"scripts"`, depois de `"migrate:prod"`:

```json
    "conta:reativar": "tsx src/scripts/reativarConta.ts",
    "conta:reativar:prod": "node dist/scripts/reativarConta.js",
```

- [ ] **Step 8: Documentar no deploy**

Em `docs/deploy.md`, antes de `## Firewall`, inserir:

````md
## Contas desativadas

`perfil › desativar conta` não apaga nada: a conta para de entrar e some da rede social. Só você,
na VPS, reativa:

```bash
docker compose exec backend npm run conta:reativar:prod -- joao@ufn.edu.br
```

Saída `conta reativada` (a pessoa já entra com a mesma senha de antes), `essa conta já está ativa`
ou `nenhuma conta com esse e-mail` (sai com código 1). Em desenvolvimento, dentro de `backend/`:
`npm run conta:reativar -- joao@ufn.edu.br`.

Para ver quem está desativado:

```bash
docker compose exec -T postgres psql -U dieta dieta -c "select email, desativada_em from users where desativada_em is not null"
```
````

- [ ] **Step 9: Build confere que o script entra no `dist`**

Run (em `backend/`): `npm test && npm run typecheck && npm run build && ls dist/scripts`
Expected: suíte PASS; build ok; lista `argumentos.js` e `reativarConta.js` (o `tsconfig` compila todo `src/**/*.ts` menos os testes, e o `Dockerfile` copia `dist` inteiro).

- [ ] **Step 10: Conferir na mão**

Com a conta de teste desativada na Task 3: `npm run conta:reativar -- <email dela>` → `conta reativada`; `POST /api/auth/login` → `200`; rodar de novo → `essa conta já está ativa`; `npm run conta:reativar` sem e-mail → mensagem de uso e código 2.

- [ ] **Step 11: Commit**

```bash
git add backend/src/scripts backend/src/repos/usuarios.ts backend/package.json docs/deploy.md
git commit -m "Script do administrador para reativar conta desativada"
```

---

### Task 6: Tela `trocar senha` (`/perfil/senha`)

**Files:**
- Create: `frontend/src/lib/senha.ts`
- Create: `frontend/src/lib/senha.test.ts`
- Modify: `frontend/src/lib/api.ts` (objeto `api`, depois de `salvarPerfil`)
- Create: `frontend/src/pages/perfil/SenhaPage.tsx`
- Modify: `frontend/src/App.tsx` (rota ao lado de `/perfil/metas`, `/perfil/dados`, `/perfil/refeicoes`)

**Interfaces:**
- Consumes: `api`, `mensagemDoErro` (`lib/api.ts`); `SubTela`, `BotaoCta`, `Erro` (plano B); `Protegida` em `App.tsx`.
- Produces: `validarNovaSenha(nova: string, confirmacao: string): string | null`; `api.trocarSenha(senha_atual: string, senha_nova: string): Promise<void>`; `api.desativarConta(senha: string): Promise<void>`.

- [ ] **Step 1: Teste que falha**

`frontend/src/lib/senha.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { validarNovaSenha } from './senha';

describe('validarNovaSenha', () => {
  it('aceita senha de 8+ caracteres confirmada igual', () => {
    expect(validarNovaSenha('abcdefgh', 'abcdefgh')).toBeNull();
  });

  it('recusa senha curta antes de olhar a confirmação', () => {
    expect(validarNovaSenha('abc', 'xyz')).toBe('a senha nova precisa de pelo menos 8 caracteres');
  });

  it('recusa senha longa demais', () => {
    const longa = 'a'.repeat(201);
    expect(validarNovaSenha(longa, longa)).toBe('a senha nova pode ter no máximo 200 caracteres');
  });

  it('recusa confirmação diferente', () => {
    expect(validarNovaSenha('abcdefgh', 'abcdefgi')).toBe('as senhas não batem');
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run (em `frontend/`): `npx vitest run src/lib/senha.test.ts`
Expected: FAIL — `Failed to resolve import "./senha"`.

- [ ] **Step 3: Implementar a validação e o cliente**

`frontend/src/lib/senha.ts`:

```ts
/** Mesmas regras do backend (8–200), checadas antes de gastar uma tentativa do limite. */
export function validarNovaSenha(nova: string, confirmacao: string): string | null {
  if (nova.length < 8) return 'a senha nova precisa de pelo menos 8 caracteres';
  if (nova.length > 200) return 'a senha nova pode ter no máximo 200 caracteres';
  if (nova !== confirmacao) return 'as senhas não batem';
  return null;
}
```

Em `frontend/src/lib/api.ts`, dentro de `export const api = { ... }`, logo depois de `salvarPerfil`:

```ts
  trocarSenha: (senha_atual: string, senha_nova: string) =>
    requisitar<void>('/me/senha', { method: 'PUT', corpo: { senha_atual, senha_nova } }),

  desativarConta: (senha: string) =>
    requisitar<void>('/me/desativar', { method: 'POST', corpo: { senha } }),
```

(Senha errada volta 400 `SENHA_INCORRETA`, então o fim de sessão automático não dispara.)

- [ ] **Step 4: Rodar o teste**

Run (em `frontend/`): `npx vitest run src/lib/senha.test.ts`
Expected: 4 testes PASS.

- [ ] **Step 5: Página**

Mesma casca das irmãs `MetasPage`/`DadosPage`: `SubTela` com `voltarPara="/perfil"` e o CTA no `rodape`. O `rodape` fica fora do `<form>`, então o botão aponta para o formulário pelo atributo `form` — Enter num campo também envia.

`frontend/src/pages/perfil/SenhaPage.tsx`:

```tsx
import { useState } from 'react';
import type { FormEvent } from 'react';
import Box from '@mui/material/Box';
import TextField from '@mui/material/TextField';
import { useNavigate } from 'react-router-dom';
import { api, mensagemDoErro } from '../../lib/api';
import { validarNovaSenha } from '../../lib/senha';
import SubTela from '../../components/perfil/SubTela';
import BotaoCta from '../../components/ui/BotaoCta';
import Erro from '../../components/Erro';

const ID_FORM = 'form-trocar-senha';
const limite = { htmlInput: { maxLength: 200 } };

export default function SenhaPage() {
  const navigate = useNavigate();
  const [atual, setAtual] = useState('');
  const [nova, setNova] = useState('');
  const [confirmacao, setConfirmacao] = useState('');
  const [erro, setErro] = useState<string | null>(null);
  const [salvando, setSalvando] = useState(false);

  async function salvar(evento: FormEvent) {
    evento.preventDefault();
    if (atual === '') return setErro('informe a senha atual');
    const invalida = validarNovaSenha(nova, confirmacao);
    if (invalida !== null) return setErro(invalida);

    setSalvando(true);
    setErro(null);
    try {
      await api.trocarSenha(atual, nova);
      navigate('/perfil', { replace: true });
    } catch (falha: unknown) {
      setErro(mensagemDoErro(falha));
      setSalvando(false);
    }
  }

  return (
    <SubTela
      titulo="trocar senha"
      voltarPara="/perfil"
      rodape={
        <BotaoCta type="submit" form={ID_FORM} disabled={salvando}>
          {salvando ? 'salvando...' : 'salvar senha'}
        </BotaoCta>
      }
    >
      {erro !== null && <Erro mensagem={erro} aoFechar={() => setErro(null)} />}
      <Box
        component="form"
        id={ID_FORM}
        onSubmit={(evento: FormEvent) => void salvar(evento)}
        sx={{ display: 'flex', flexDirection: 'column', gap: '12px' }}
      >
        <TextField
          label="senha atual"
          type="password"
          autoFocus
          autoComplete="current-password"
          value={atual}
          onChange={(e) => setAtual(e.target.value)}
          slotProps={limite}
        />
        <TextField
          label="senha nova"
          type="password"
          autoComplete="new-password"
          value={nova}
          onChange={(e) => setNova(e.target.value)}
          helperText="pelo menos 8 caracteres"
          slotProps={limite}
        />
        <TextField
          label="confirmar senha nova"
          type="password"
          autoComplete="new-password"
          value={confirmacao}
          onChange={(e) => setConfirmacao(e.target.value)}
          slotProps={limite}
        />
      </Box>
    </SubTela>
  );
}
```

(`BotaoCta` espalha as props no `ButtonBase`, que renderiza `<button>`, então `type` e `form` chegam no elemento. Se o typecheck do MUI 9 recusar `form`, trocar `type`/`form` por `onClick={() => (document.getElementById(ID_FORM) as HTMLFormElement | null)?.requestSubmit()}`.)

- [ ] **Step 6: Rota**

Em `frontend/src/App.tsx`, junto dos imports das subtelas do plano B:

```tsx
import SenhaPage from './pages/perfil/SenhaPage';
```

e logo depois de `<Route path="/perfil/refeicoes" … />` (irmã da `Casca`, sem tab bar):

```tsx
<Route path="/perfil/senha" element={<Protegida><SenhaPage /></Protegida>} />
```

- [ ] **Step 7: Verificar**

Run (em `frontend/`): `npm test && npm run typecheck && npm run build`
Expected: testes PASS, sem erros de tipo, build ok.

- [ ] **Step 8: Commit**

```bash
git add frontend/src/lib/senha.ts frontend/src/lib/senha.test.ts frontend/src/lib/api.ts frontend/src/pages/perfil/SenhaPage.tsx frontend/src/App.tsx
git commit -m "Subtela de trocar senha no perfil"
```

---

### Task 7: `desativar conta` no perfil + fim de sessão com aviso no login

**Files:**
- Create: `frontend/src/lib/sessao.ts`
- Create: `frontend/src/lib/sessao.test.ts`
- Modify: `frontend/src/lib/api.ts` (`requisitar` e exports)
- Modify: `frontend/src/pages/Login.tsx` (versão do plano B, Task 3)
- Create: `frontend/src/components/perfil/DialogoDesativarConta.tsx`
- Modify: `frontend/src/pages/Perfil.tsx` (chamada de `SecaoConta`)

**Interfaces:**
- Consumes: `api.desativarConta(senha)` (Task 6); `useAuth()` (`sair`, `carregando`); `SecaoConta`, `LinhaLista`, `BotaoCta`, `Erro` (plano B).
- Produces: `encerraSessao(status: number, codigo: string, semAuth: boolean): boolean`; em `lib/api.ts`, `definirMotivoSaida(mensagem: string): void` e `consumirMotivoSaida(): string | null`; `DialogoDesativarConta({ aberto: boolean; aoFechar: () => void })` (default export).

- [ ] **Step 1: Teste que falha**

`frontend/src/lib/sessao.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { encerraSessao } from './sessao';

describe('encerraSessao', () => {
  it('401 de rota autenticada derruba a sessão', () => {
    expect(encerraSessao(401, 'NAO_AUTORIZADO', false)).toBe(true);
  });

  it('conta desativada derruba a sessão', () => {
    expect(encerraSessao(403, 'CONTA_DESATIVADA', false)).toBe(true);
  });

  it('outro 403 não derruba', () => {
    expect(encerraSessao(403, 'SEM_ACESSO', false)).toBe(false);
  });

  it('senha errada (400) não derruba', () => {
    expect(encerraSessao(400, 'SENHA_INCORRETA', false)).toBe(false);
  });

  it('login e cadastro (sem auth) nunca derrubam: o erro aparece no formulário', () => {
    expect(encerraSessao(401, 'CREDENCIAIS_INVALIDAS', true)).toBe(false);
    expect(encerraSessao(403, 'CONTA_DESATIVADA', true)).toBe(false);
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run (em `frontend/`): `npx vitest run src/lib/sessao.test.ts`
Expected: FAIL — `Failed to resolve import "./sessao"`.

- [ ] **Step 3: Implementar**

`frontend/src/lib/sessao.ts`:

```ts
/** Resposta que significa "essa sessão acabou": token inválido ou conta desativada. */
export function encerraSessao(status: number, codigo: string, semAuth: boolean): boolean {
  if (semAuth) return false;
  return status === 401 || codigo === 'CONTA_DESATIVADA';
}
```

Em `frontend/src/lib/api.ts`:

1. Import no topo: `import { encerraSessao } from './sessao';`
2. Logo depois de `export function registrarLogout(…) { … }`:

```ts
let motivoSaida: string | null = null;

/** Guarda por que a sessão acabou, para o login mostrar uma vez. */
export function definirMotivoSaida(mensagem: string): void {
  motivoSaida = mensagem;
}

export function consumirMotivoSaida(): string | null {
  const motivo = motivoSaida;
  motivoSaida = null;
  return motivo;
}
```

3. Em `requisitar`, trocar:

```ts
  if (!resposta.ok) {
    const erro = extrairErro(corpo, resposta.status);
    if (resposta.status === 401 && opcoes.semAuth !== true) aoDeslogar();
    throw erro;
  }
```

por:

```ts
  if (!resposta.ok) {
    const erro = extrairErro(corpo, resposta.status);
    if (encerraSessao(resposta.status, erro.code, opcoes.semAuth === true)) {
      if (erro.code === 'CONTA_DESATIVADA') definirMotivoSaida(erro.message);
      aoDeslogar();
    }
    throw erro;
  }
```

- [ ] **Step 4: Rodar o teste**

Run (em `frontend/`): `npx vitest run src/lib/sessao.test.ts`
Expected: 5 testes PASS.

- [ ] **Step 5: Login mostra o motivo**

O `Login` pode montar enquanto a sessão ainda carrega (a rota `/login` renderiza durante `carregando`), e o motivo só existe depois que o `GET /api/me` falha. Por isso lê quando `carregando` vira `false`.

Em `frontend/src/pages/Login.tsx`:

1. Trocar `import { useState } from 'react';` por `import { useEffect, useState } from 'react';` e `import { mensagemDoErro } from '../lib/api';` por `import { consumirMotivoSaida, mensagemDoErro } from '../lib/api';`.
2. Trocar `const { entrar, cadastrar } = useAuth();` por `const { entrar, cadastrar, carregando } = useAuth();`.
3. Logo depois de `const [enviando, setEnviando] = useState(false);`:

```tsx
  useEffect(() => {
    if (carregando) return;
    const motivo = consumirMotivoSaida();
    if (motivo !== null) setErro(motivo);
  }, [carregando]);
```

(Tentar entrar numa conta desativada já mostra a mensagem pelo caminho normal: `entrar` lança o `403` e o `catch` do formulário exibe `mensagemDoErro`.)

- [ ] **Step 6: Diálogo**

Mesmo `Paper` e mesmos botões do `useConfirmacao` do plano B (cancelar em `contorno` com foco, ação pintada de `error.main`), mais o campo de senha.

`frontend/src/components/perfil/DialogoDesativarConta.tsx`:

```tsx
import { useState } from 'react';
import type { FormEvent } from 'react';
import Box from '@mui/material/Box';
import Dialog from '@mui/material/Dialog';
import TextField from '@mui/material/TextField';
import Typography from '@mui/material/Typography';
import { useNavigate } from 'react-router-dom';
import { useAuth } from '../../auth/useAuth';
import { api, definirMotivoSaida, mensagemDoErro } from '../../lib/api';
import Erro from '../Erro';
import BotaoCta from '../ui/BotaoCta';

interface Props {
  aberto: boolean;
  aoFechar: () => void;
}

export default function DialogoDesativarConta({ aberto, aoFechar }: Props) {
  const { sair } = useAuth();
  const navigate = useNavigate();
  const [senha, setSenha] = useState('');
  const [erro, setErro] = useState<string | null>(null);
  const [desativando, setDesativando] = useState(false);

  function fechar() {
    if (desativando) return;
    setSenha('');
    setErro(null);
    aoFechar();
  }

  async function desativar(evento: FormEvent) {
    evento.preventDefault();
    if (senha === '') return setErro('informe a senha');
    setDesativando(true);
    setErro(null);
    try {
      await api.desativarConta(senha);
      definirMotivoSaida('sua conta foi desativada. para voltar, fale com o administrador');
      sair();
      navigate('/login', { replace: true });
    } catch (falha: unknown) {
      setErro(mensagemDoErro(falha));
      setDesativando(false);
    }
  }

  return (
    <Dialog
      open={aberto}
      onClose={fechar}
      slotProps={{
        paper: {
          sx: { borderRadius: '20px', p: '22px', m: '22px', bgcolor: 'background.default', backgroundImage: 'none' },
        },
      }}
    >
      <Box
        component="form"
        onSubmit={(evento: FormEvent) => void desativar(evento)}
        sx={{ display: 'flex', flexDirection: 'column', gap: '14px' }}
      >
        <Typography sx={{ fontWeight: 700, fontSize: 20, letterSpacing: '-.02em' }}>desativar conta</Typography>
        <Typography sx={{ fontSize: 13.5, lineHeight: 1.45, color: 'text.secondary' }}>
          sua conta fica desativada: você não consegue mais entrar e some para amigos e grupos. nada é apagado.
          para voltar, fale com o administrador.
        </Typography>
        {erro !== null && <Erro mensagem={erro} aoFechar={() => setErro(null)} />}
        <TextField
          label="sua senha"
          type="password"
          autoComplete="current-password"
          value={senha}
          onChange={(e) => setSenha(e.target.value)}
          slotProps={{ htmlInput: { maxLength: 200 } }}
        />
        <Box sx={{ display: 'flex', gap: '9px' }}>
          {/* Cancelar vem primeiro e leva o foco: Enter afobado não desativa nada. */}
          <BotaoCta variante="contorno" autoFocus disabled={desativando} onClick={fechar} sx={{ flex: 1 }}>
            cancelar
          </BotaoCta>
          <BotaoCta
            type="submit"
            variante="primario"
            disabled={desativando}
            sx={{ flex: 1, bgcolor: 'error.main', '&:hover': { bgcolor: 'error.main', filter: 'brightness(.92)' } }}
          >
            {desativando ? 'desativando...' : 'desativar'}
          </BotaoCta>
        </Box>
      </Box>
    </Dialog>
  );
}
```

- [ ] **Step 7: Linhas no perfil via `SecaoConta`**

Em `frontend/src/pages/Perfil.tsx` (versão do plano B, Task 10):

1. Import novo (`useState`, `useNavigate`, `LinhaLista` e `SecaoConta` já estão importados):

```tsx
import DialogoDesativarConta from '../components/perfil/DialogoDesativarConta';
```

2. Junto dos outros estados, **antes** do `if (perfil === null) return null;` (regra dos hooks), logo depois de `const [salvandoModo, setSalvandoModo] = useState(false);`:

```tsx
  const [desativandoConta, setDesativandoConta] = useState(false);
```

3. Trocar a última linha de dentro de `<Tela gap={26}>`, que hoje é `<SecaoConta />`, por:

```tsx
      <SecaoConta
        antesDeSair={<LinhaLista rotulo="trocar senha" chevron onClick={() => navigate('/perfil/senha')} />}
        depoisDeSair={
          <LinhaLista rotulo="desativar conta" cor="perigo" ultima onClick={() => setDesativandoConta(true)} />
        }
      />
      <DialogoDesativarConta aberto={desativandoConta} aoFechar={() => setDesativandoConta(false)} />
```

Com `depoisDeSair` preenchido, a `SecaoConta` já tira o `ultima` de `sair`.

- [ ] **Step 8: Verificar**

Run (em `frontend/`): `npm test && npm run typecheck && npm run build`
Expected: tudo verde.

- [ ] **Step 9: Conferir no navegador**

Backend e frontend em `npm run dev`, conta de teste, DevTools em 390×844, claro **e** escuro:
- `perfil` mostra em CONTA: `trocar senha ›`, `sair`, `desativar conta` (as duas últimas em `error.main`).
- `trocar senha`: abre sem tab bar; senha atual errada mostra "senha atual incorreta" e **continua logado**; senhas diferentes mostram "as senhas não batem" sem chamar a API; com tudo certo volta ao perfil.
- `desativar conta`: cancelar fecha e limpa a senha; senha errada mostra o erro e continua logado; senha certa cai no `/login` com a faixa "sua conta foi desativada. para voltar, fale com o administrador"; tentar entrar de novo mostra "conta desativada — fale com o administrador".
- Sessão derrubada em outro aparelho: com a conta logada em duas abas, desativar numa; na outra, recarregar → cai no `/login` com "conta desativada — fale com o administrador".
- Reativar pelo script (Task 5) → login volta a funcionar.

- [ ] **Step 10: Commit**

```bash
git add frontend/src/lib/sessao.ts frontend/src/lib/sessao.test.ts frontend/src/lib/api.ts frontend/src/pages/Login.tsx frontend/src/components/perfil/DialogoDesativarConta.tsx frontend/src/pages/Perfil.tsx
git commit -m "Desativar conta no perfil e aviso de conta desativada no login"
```

---

### Task 8: Revisão final

**Files:** nenhum novo.

- [ ] **Step 1: Suítes completas**

Run (em `backend/`): `npm test && npm run typecheck && npm run build`
Run (em `frontend/`): `npm test && npm run typecheck && npm run build`
Expected: tudo verde.

- [ ] **Step 2: Contrato x código**

Conferir que `docs/api-contract.md` bate com o código: caminhos (`/api/me/senha`, `/api/me/desativar`), campos (`senha_atual`, `senha_nova`, `senha`), limites (8–200; 10 por 15 min), códigos (`SENHA_INCORRETA` 400, `CONTA_DESATIVADA` 403, `LIMITE_EXCEDIDO` 429), mensagens ("senha atual incorreta", "conta desativada — fale com o administrador") e a lista de efeitos no social.

- [ ] **Step 3: Nenhuma consulta social esqueceu o filtro**

Run (em `backend/`): `git grep -n "JOIN users\|FROM users" -- src/repos src/routes`
Expected: toda ocorrência em `repos/social.ts`, `repos/registros.ts` (feed) e `repos/interacoes.ts` (comentários) tem `desativada_em IS NULL` na mesma consulta. As de `repos/usuarios.ts` sem filtro são intencionais: `buscarPorId`/`buscarPorEmail` (o middleware e o login precisam enxergar a conta para barrar), `tagLivrePara` (tag continua ocupada), `contarUsuarios`, `reativarPorEmail`. Anotar qualquer outra.

- [ ] **Step 4: Frontend usa só as peças do plano B**

Run (em `frontend/`): `git grep -n "className\|ListItemButton\|InputBase" -- src/pages/perfil/SenhaPage.tsx src/components/perfil/DialogoDesativarConta.tsx src/pages/Perfil.tsx`
Expected: sem resultado.

Run: `git grep -n "excluirConta\|excluir conta\|DELETE.*'/me'" -- src ../backend/src ../docs`
Expected: sem resultado (nenhuma sobra da versão "excluir").

- [ ] **Step 5: Revisão de segurança**

Pela tabela de roteamento do CLAUDE.md, este plano toca auth: rodar o `security-reviewer` (ou `/security-review`) sobre o diff dos commits deste plano antes do merge, conferindo que a senha nunca é logada, que `CONTA_DESATIVADA` só aparece no login depois de a senha conferir, que o middleware barra tokens antigos, que o limite vale para os dois endpoints e que o script de reativação não fica exposto por HTTP.
